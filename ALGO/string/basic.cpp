#include <stdio.h>
#include <string>


int main(int argc, char const *argv[]) {
    string s;
    string sub;
    scanf("%s",&s);
    scanf("%s",&sub);
    for(int i=0;i<s.length();i++){
        if(s[i])
        for(int j=0)



    }
    return 0;
}
